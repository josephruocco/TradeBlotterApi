services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: blotter
      POSTGRES_PASSWORD: blotter
      POSTGRES_DB: tradeblotter
    ports:
      - "5432:5432"
    volumes:
      - tradeblotter_pg:/var/lib/postgresql/data

volumes:
  tradeblotter_pg: